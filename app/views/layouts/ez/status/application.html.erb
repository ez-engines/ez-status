<!DOCTYPE html>
<html>
<head>
  <title>Ez::Status</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width">

  <%#= csrf_meta_tags %>
  <%#= csp_meta_tag %>

  <%#= stylesheet_link_tag    "ez/status/application", media: "all" %>
  <style type="text/css" media="screen">
      body {
          margin: 0;
          background-color: #eaeaea;
          display: grid;
          /*height: 100vh;*/
          place-items: center;
          font-family: 'Source Sans Pro', sans-serif;
      }
      .status {
          margin-top: 50px;
          max-width: 800px;
          width: 100%;
          border-radius: 12px;
          box-shadow: 0 0 40px -10px rgba(0, 0, 0, .4);
      }

      .status header {
          --start: 15%;
          height: 130px;
          background-image: repeating-radial-gradient(circle at var(--start), transparent 0%, transparent 10%, rgba(54, 89, 219, .33) 10%, rgba(54, 89, 219, .33) 17%), linear-gradient(to right, #5b7cfa, #3659db);
          color: #fff;
          position: relative;
          border-radius: 12px 12px 0 0;
          overflow: hidden;
      }

      .status header .status__title {
          position: absolute;
          z-index: 2;
          top: 50%;
          right: calc(var(--start) * .75);
          transform: translateY(-50%);
          text-transform: uppercase;
          margin: 0;
      }

      .status header .status__title span {
          display: block;
      }

      .status header .status__title--top {
          font-size: 24px;
          font-weight: 700;
          letter-spacing: 6.5px;
      }

      .status header .status__title--bottom {
          font-size: 13px;
          font-weight: 500;
          letter-spacing: 3.55px;
          opacity: 0.65;
          transform: translateY(-2px);
      }

      .status header .status__icon {
          fill: #fff;
          opacity: 0.35;
          width: 50px;
          position: absolute;
          top: 50%;
          left: var(--start);
          transform: translate(-50%, -50%);
      }

      .status__profiles {
          background-color: #fff;
          border-radius: 0 0 12px 12px;
          padding: 15px 15px 20px;
          display: grid;
          row-gap: 8px;
      }

      .status__profile {
          display: grid;
          grid-template-columns: 1fr 3fr 2fr;
          align-items: center;
          padding: 10px 30px 10px 10px;
          overflow: hidden;
          border-radius: 10px;
          box-shadow: 0 5px 7px -1px rgba(51, 51, 51, 0.23);
          cursor: pointer;
          transition: transform 0.25s cubic-bezier(0.7, 0.98, 0.86, 0.98), box-shadow 0.25s cubic-bezier(0.7, 0.98, 0.86, 0.98);
          background-color: #fff;
      }

      .status__profile:hover {
          transform: scale(1.01);
          box-shadow: 0 9px 47px 11px rgba(51, 51, 51, 0.18);
      }

      .status__picture {
          max-width: 100%;
          width: 60px;
          border-radius: 25%;
          box-shadow: 0 0 0 10px #ebeef3, 0 0 0 22px #f3f4f6;
      }

      .status__name {
          color: #979cb0;
          font-weight: 600;
          font-size: 20px;
          letter-spacing: 0.64px;
          margin-left: 12px;
      }

      .status__value {
          color: #35d8ac;
          font-weight: 700;
          font-size: 34px;
          text-align: right;
      }

      .failed {
          color: #a0050a;
      }

      .status__value > span {
          opacity: 0.8;
          font-weight: 600;
          font-size: 13px;
          margin-left: 3px;
      }
  </style>
</head>
<body>
<%= yield %>
</body>
</html>
